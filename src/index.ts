/**
 * UX Toolkit - OpenCode Plugin
 *
 * Only exports the plugin default to avoid "hook.config" errors.
 * Library functions are available via direct imports from the package.
 */

export { default } from './plugin.js';
